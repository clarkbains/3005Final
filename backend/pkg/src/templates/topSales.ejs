<div>
    <table>
        <% for(let salesItem of data) {%>
            <tr>
              <td><%= salesItem.name ?? "[Unknown]" %> </td>
              <td><%= Number(salesItem.amount).toFixed(2) %></td>
            </tr>
         <% } %>
    </table>
</div>